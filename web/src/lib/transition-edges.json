{
  "$comment": "Mirror of packages/jinn/src/work-items/transitions.ts — the declared status edges plus the rule constants legalTargets() derives from. The gateway module is the truth; packages/jinn/src/work-items/__tests__/board-legality-parity.test.ts probes transition() behaviorally against this file and fails on any drift, so this mirror can never rot silently. Edit transitions.ts first, then regenerate this file to match.",
  "edges": {
    "backlog": ["assigned", "executing", "in_review", "blocked", "done", "cancelled", "escalated"],
    "assigned": ["backlog", "executing", "in_review", "blocked", "done", "cancelled", "escalated"],
    "executing": ["in_review", "blocked", "done", "cancelled", "escalated"],
    "in_review": ["executing", "done", "blocked", "cancelled", "escalated"],
    "blocked": ["backlog", "assigned", "executing", "in_review", "done", "cancelled", "escalated"],
    "escalated": ["backlog", "assigned", "executing", "in_review", "done", "blocked", "cancelled"],
    "done": ["backlog"],
    "cancelled": ["backlog"]
  },
  "manualExecutingFrom": ["backlog", "assigned"],
  "sticky": ["done", "cancelled", "escalated"],
  "closeGated": ["done", "cancelled"]
}
