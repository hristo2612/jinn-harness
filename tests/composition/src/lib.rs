//! Support for the real-composition gate: locating a `jinnd` checkout,
//! building the PINNED daemon, building the cron kit, and driving a live
//! daemon process. The tests live in `tests/`.

pub mod daemon;
pub mod kit;
